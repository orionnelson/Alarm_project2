1. First copy the files "alarm_cond.c", and "errors.h" into your
   own directory.

2. To compile the program "new_alarm_cond.c", use the following command:

      make

3. Type "./My_Alarm" to run the executable code.

4. At the prompt "ALARM>", type in the specific alarm request that is needed.
   These commands will perform the tasks mentioned inside the assignment rubric.

   ALARM> <time> Message(<alarm ID>) <message>
   ALARM> Cancel: Message(<alarm ID>)

  (To exit from the program, type Ctrl-d.)
